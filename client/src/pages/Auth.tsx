import AuthForm from "@/components/forms/AuthForm";

const Auth = () => {
  return (
    <div className="flex-center overflow-x-hidden overflow-y-auto max-h-[90%] transform bg-blue">
      <AuthForm />
    </div>
  );
};

export default Auth;
