// import * as dotenv from "dotenv";

// dotenv.config();
// { path: __dirname + "../.env.local" }
const config = {
  apiURL: "/api/" || "",
  conversation: {
    fetchLimit: 50,
  },
};

export default config;
